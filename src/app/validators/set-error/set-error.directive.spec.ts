import { SetErrorDirective } from './set-error.directive';

describe('SetErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new SetErrorDirective();
    expect(directive).toBeTruthy();
  });
});
