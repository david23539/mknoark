import {MaxNumberFilesDirective} from './max-number-files.directive';

describe('MaxNumberFilesDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxNumberFilesDirective();
    expect(directive).toBeTruthy();
  });
});
