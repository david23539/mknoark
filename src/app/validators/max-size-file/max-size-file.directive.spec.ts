import {MaxSizeFileDirective} from './max-size-file.directive';

describe('MaxSizeFileDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxSizeFileDirective();
    expect(directive).toBeTruthy();
  });
});
