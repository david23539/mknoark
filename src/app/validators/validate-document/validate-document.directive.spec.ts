import {ValidateDocumentDirective} from './validate-document.directive';

describe('ValidateDocumentDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateDocumentDirective();
    expect(directive).toBeTruthy();
  });
});
