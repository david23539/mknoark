import { NumberMaxDirective } from './number-max.directive';

describe('NumberMaxDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberMaxDirective();
    expect(directive).toBeTruthy();
  });
});
